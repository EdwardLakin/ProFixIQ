// Legacy feature path kept for compatibility with existing imports.
// Canonical feature path: @/features/stripe/api/stripe/webhook/route
export { runtime, handleStripeWebhook, POST } from "../../webhook/route";
