"use client";
import type { WorkforceActivityFeedItem } from "../lib/activityTypes";
function format(v: string, timezone?: string | null) { return new Intl.DateTimeFormat(undefined,{ hour:"numeric", minute:"2-digit", timeZone: timezone || undefined }).format(new Date(v)); }
export function WorkforceActivityFeed({ items, timezone }: { items: WorkforceActivityFeedItem[]; timezone?: string | null }) { return <div className="space-y-2">{items.length ? items.map(i=><div key={i.id} className="rounded-lg border border-white/10 bg-black/20 p-3 text-sm"><span className="text-white">{format(i.timestamp, timezone)}</span><span className="text-neutral-400"> — </span><span className="text-neutral-200">{i.employeeName} {i.action}</span>{i.workOrderNumber ? <span className="text-orange-200"> WO {i.workOrderNumber}</span> : null}{i.lineDescription ? <span className="text-neutral-400"> · {i.lineDescription}</span> : null}</div>) : <p className="text-sm text-neutral-400">No operational events available.</p>}</div>; }
