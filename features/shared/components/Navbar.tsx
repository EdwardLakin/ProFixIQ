// components/Navbar.tsx
"use client";

// Intentionally render nothing so existing imports don't break.
// Top navbar has been retired in favor of the left sidebar + tabs bar.
export default function Navbar(): null {
  return null;
}