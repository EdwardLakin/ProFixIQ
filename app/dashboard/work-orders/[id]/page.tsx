export const dynamic = "force-dynamic";
export const revalidate = 0;

// Re-export the Work Order detail page inside the dashboard layout.
export { default } from "@/features/work-orders/app/work-orders/[id]/page";