export const dynamic = "force-dynamic";
export const revalidate = 0;

// Re-export the Work Orders menu (tiles) so it renders inside the dashboard layout.
export { default } from "@/features/work-orders/app/work-orders/page";