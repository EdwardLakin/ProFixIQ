# Owner field capability rule

`owner` remains an account/management role. Mobile field execution is opt-in per profile via `mobile_field_operators`. The capability creates schedulable technician capacity for that profile but does not give every owner technician behavior. Financial actions granted through Mobile V1 are further limited to work orders attached to Service Visits assigned to that profile.
